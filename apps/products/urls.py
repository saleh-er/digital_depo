from django.urls import path
from . import views

app_name = 'products'

urlpatterns = [
    # We will leave this empty for a moment until we write our first view
    # path('', views.ProductListView.as_view(), name='list'),
]